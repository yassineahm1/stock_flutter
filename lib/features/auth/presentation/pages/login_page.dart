import 'package:flutter/material.dart';

/// Page de Login - couche Presentation
/// Équivalent à presentation/MainActivity.java du professeur
/// (Page en cours de développement - sera complétée dans les prochaines étapes)
class LoginPage extends StatelessWidget {
  const LoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Login Page - En cours de développement'),
      ),
    );
  }
}
